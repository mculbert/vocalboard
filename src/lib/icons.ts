/**
 * Semantic icon map: vocalboard feature names → @lucide/svelte components.
 *
 * Swap this file to change icon sets project-wide without touching components.
 * Only import and re-export icons that are actually used; tree-shaking handles
 * the rest.
 */
export {
	Play,
	Pause,
	Square as Stop,
	Scissors as Cut,
	VolumeX as Mute,
	Undo2 as Undo,
	Redo2 as Redo,
	Settings,
	FolderOpen,
	FileAudio,
	Download,
	Loader2 as Spinner,
	AlertCircle as Error,
	CheckCircle2 as Success,
} from '@lucide/svelte';
